@using VerificadorCartaoCredito.Models
@model CartaoCredito
@{
    ViewBag.Title = "ValidarCartao";
}


<div class="row"> 
    <div class="row">
        <div class="col-md-8">
            <section id="validarNumeroCartao">
                @*using (Html.BeginForm("", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                *@
                <!--form action="/Controllers/HomeController" method="post"-->
                @using (Html.BeginForm("Index", "Home", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                

                    @Html.AntiForgeryToken()
                    <h4>Digite o número do Cartão de Crédito:</h4>
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        @*Html.LabelFor(c => c.NumeroCartaoCredito, new { @class = "col-md-2 control-label" })*@
                        <div class="col-md-10">
                            @*Html.TextBoxFor(c => c.NumeroCartaoCredito, new { @class = "form-control" })*@
                            @Html.EditorFor(c => c.NumeroCartaoCredito, new { htmlAttributes = new { @class = "form-control" } })
                            @*Html.ValidationMessageFor(c => c.NumeroCartaoCredito, "", new { @class = "text-danger" })*@
                            <label class="text-danger">@ViewBag.Message</label>
                        </div>
                    </div>


                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Validar" class="btn btn-default" />
                        </div>
                    </div>
                <!--</form>-->
                
                }
                
            </section>
        </div>
    </div>
</div>


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}